export default class Project {
  constructor({
    name,
    site,
    code,
    img,
    description
  }) {
    this.name = name;
    this.site = site;
    this.code = code;
    this.img = img;
    this.description = description;
  }
}
